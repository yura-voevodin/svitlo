<main>
    <div class="container px-4 py-2">
        <h1>Івано-Франківськ - графік погодинних вимкнень, <%= l Date.today, format: "%A, %e %b %Y" %></h1>

        <ul>
            <% @lines.each do |line| %>
            <li>
                <%= link_to line.name, line %>
            </li>
            <% end %>
        </ul>

    </div>
</main>
